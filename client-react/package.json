{
  "name": "client-react",
  "version": "1.0.0",
  "description": "This is a blank project for CDK development with Python.",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "dev": "cd app && yarn dev",
    "build": "cd app && yarn build",
    "predeploy": "yarn build",
    "deploy": "cdk bootstrap && cdk synth && cdk deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}